let paper = new joint.dia.Paper({

    el: $('#paper'),
    width: 550,
    height: 300,
    gridSize: 1,
    model: graph,
    gridSize: 1,
    drawGrid: true, // default pattern (dot) with default settings
});